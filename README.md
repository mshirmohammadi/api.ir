# api.ir
ارائه انواع وب سرویس های OTP، احراز هویت، استعلام ها و ....
# ShahkarServicePHP

یک کلاس PHP برای ارتباط با وب‌سرویس‌های Shahkar (استعلام ملی، بانک، قبض، خودرو و غیره).

## نصب
کافیست کلاس را در پروژه خود `require` یا `include` کنید:

```php
require 'Api_Services.php';

$shahkar = new ShahkarService("توکن_شما");
$result = $shahkar->getPowerBill("1100151403410");
print_r($result);



عالی! با توجه به کدی که فرستادی و توضیحاتی که دادی، من می‌توانم یک README حرفه‌ای و کامل بسازم که برای هر متد:

اسم متد

توضیح کوتاه

پارامترها

نمونه استفاده (PHP)

نمونه خروجی JSON

را داشته باشد و کاملاً آماده‌ی قرار دادن روی GitHub باشد.

در ادامه یک نسخه نمونه حرفه‌ای برای چند متد اول می‌سازم تا قالب مشخص شود. بعد از آن می‌توانیم ادامه‌ی تمام متدها را با همین قالب بسازیم:

# ShahkarService PHP Library

کتابخانه PHP برای اتصال به وب‌سرویس‌های شاهکار.  
با استفاده از این پکیج می‌توانید استعلام هویت، پیامک و تماس OTP، احراز هویت و بایومتریک، اطلاعات بانکی، قبض و خدمات پستی را انجام دهید.

---

## 📦 نصب

```bash
composer require your-username/shahkarservicephp

require 'vendor/autoload.php';

use ShahkarService\ShahkarService;

$token = "توکن_شما";
$shahkar = new ShahkarService($token);

⚡ متدها
1️⃣ checkNationalCode

توضیح: احراز هویت شاهکار (Full)
HTTP Method: POST

پارامترها:

nationalCode (string) – کد ملی فرد یا شناسه ملی شرکت

mobile (string) – شماره موبایل

isCompany (bool, اختیاری) – true برای شرکت، false برای فرد (پیش‌فرض: false)

نمونه استفاده:

$result = $shahkar->checkNationalCode("0010007700", "09120000000");
print_r($result);


نمونه خروجی:

{
  "success": true,
  "code": 1,
  "data": { "name": "علی رضایی", "status": "فعال" }
}

2️⃣ checkNationalCodeLite

توضیح: احراز هویت شاهکار Lite
HTTP Method: POST

پارامترها:

nationalCode (string) – کد ملی

mobile (string) – شماره موبایل

نمونه استفاده:

$result = $shahkar->checkNationalCodeLite("0010007700", "09120000000");
print_r($result);


نمونه خروجی:

{
  "success": true,
  "code": 1,
  "data": { "verified": true }
}