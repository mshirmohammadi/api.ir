# API_Services PHP Library

با استفاده از این پکیج میتوانید استعلامات زیر را در کسری از ثانیه دریافت نمایید.

## 📦 نصب

کافی است با استفاده از کامپوزر دستور زیر را اجرا نمایید.

```php
composer require mshirmohammadi/api.ir  
```

## 📦 راه اندازی
```php
require 'vendor/autoload.php';
use Shirmohammadi\Api_Services\Api_Services;
$token = "YOUR_API_TOKEN";
$api = new Api_Services($token);
```

---

## ⚡ متدها

### 1️⃣ احراز هویت شاهکار​ اشخاص حقیقی و حقوقی (Shahkar1)
**توضیح:** وب سرویس شاهکار تطبیق کد ملی با شماره موبایل را استعلام می نماید. در این نسخه دیتا به صورت رمز شده استعلام می گردد. همچنین امکان استعلام شناسه ملی و سیم کارت اشخاص حقوقی هم دارد

**پارامترها:**
- `nationalCode` (string) – کد ملی فرد یا شناسه ملی شرکت
- `mobile` (string) – شماره موبایل
- `isCompany` (bool, اختیاری) – true برای شرکت، false برای فرد (پیش‌فرض: false)

**نمونه استفاده:**
```php
$nationalCode   = "11111111111";
$mobile         = "09120000000";
$result         = $api->Shahkar1($nationalCode, $mobile,true);
print_r($result);
```

**نمونه پاسخ:**
```json
{
  "data": true,
  "success": true,
  "code": 1,
  "error": null,
  "message": null
}
```

عالی! من می‌تونم برایت تمام ۹ متد باقی‌مانده را طبق همان قالب اولین متد (Shahkar1) در فرمت Markdown مناسب README.MD آماده کنم تا فایل کامل و قابل دانلود داشته باشی. تمام کدهای PHP داخل ````phpو پاسخ JSON داخلjson``` قرار می‌گیرند.

در ادامه، متن کامل برای ۹ متد باقی‌مانده آماده است:

2️⃣ احراز هویت شاهکار Lite (ShahkarLite)

توضیح: نسخه Lite وب سرویس شاهکار تطبیق کد ملی با شماره موبایل را استعلام می کند. دیتا به صورت رمز شده نیست و هزینه پایین‌تری دارد.

پارامترها:

nationalCode (string) – کد ملی

mobile (string) – شماره موبایل

نمونه استفاده:

$nationalCode   = "0010007700";
$mobile         = "09120000000";
$result         = $api->ShahkarLite($nationalCode, $mobile);
print_r($result);


نمونه پاسخ:

{
  "data": true,
  "success": true,
  "code": 1,
  "error": null,
  "message": null
}

3️⃣ استعلام عکس هویتی (PersonImage)

توضیح: با دریافت کد ملی، تاریخ تولد و سریال کارت ملی، عکس کارت ملی را از ثبت احوال استعلام می‌کند. نیازمند مجوز است.

پارامترها:

nationalCode (string) – کد ملی

birthDate (string) – تاریخ تولد به فرمت YYYY/MM/DD

serialNumber (string) – سریال پشت کارت ملی یا رهگیری رسید کارت ملی

نمونه استفاده:

$nationalCode   = "0010007700";
$birthDate      = "1371/1/1";
$serialNumber   = "i1R8389398";
$result         = $api->PersonImage($nationalCode, $birthDate, $serialNumber);
print_r($result);


نمونه پاسخ:

{
  "data": {
    "imageBase64": "iVBORw0KGgoAAAANSUhEUgAA..."
  },
  "success": true,
  "code": 1,
  "error": null,
  "message": null
}

4️⃣ تطبیق کد ملی با کارت بانکی (CardMatch)

توضیح: بررسی می‌کند که کارت بانکی متعلق به کد ملی ارائه شده است یا خیر.

پارامترها:

nationalCode (string) – کد ملی

birthDate (string) – تاریخ تولد به فرمت YYYY/MM/DD

cardNumber (string) – شماره کارت بانکی

نمونه استفاده:

$nationalCode   = "0010007700";
$birthDate      = "1371/1/1";
$cardNumber     = "6037990000000000";
$result         = $api->CardMatch($nationalCode, $birthDate, $cardNumber);
print_r($result);


نمونه پاسخ:

{
  "data": true,
  "success": true,
  "code": 1,
  "error": null,
  "message": null
}

5️⃣ تطبیق کد ملی با شبا (IbanMatch)

توضیح: بررسی می‌کند که شماره شبا متعلق به کد ملی ارائه شده است یا خیر.

پارامترها:

nationalCode (string) – کد ملی

birthDate (string) – تاریخ تولد

iban (string) – شماره شبا 26 رقمی

نمونه استفاده:

$nationalCode   = "0010007700";
$birthDate      = "1371/1/1";
$iban           = "IR820540102680020817909002";
$result         = $api->IbanMatch($nationalCode, $birthDate, $iban);
print_r($result);


نمونه پاسخ:

{
  "data": true,
  "success": true,
  "code": 1,
  "error": null,
  "message": null
}

6️⃣ تطبیق کد ملی با شبا پرو (IbanMatchPro)

توضیح: بررسی سریع و بدون تاریخ تولد، تطبیق شبا و کد ملی برای فرآیندهای KYC و مالی.

پارامترها:

nationalCode (string) – کد ملی

iban (string) – شماره شبا

نمونه استفاده:

$nationalCode   = "0010007700";
$iban           = "IR820540102680020817909002";
$result         = $api->IbanMatchPro($nationalCode, $iban);
print_r($result);


نمونه پاسخ:

{
  "data": true,
  "success": true,
  "code": 1,
  "error": null,
  "message": null
}

7️⃣ وب سرویس تماس تلفنی (Call)

توضیح: برقراری تماس با مشتری جهت اعلام پیام‌های طولانی با امکان انتخاب لهجه و موسیقی پس‌زمینه.

پارامترها:

voiceID (string) – شناسه فایل صوتی

numbers (array[string]) – لیست شماره‌ها

نمونه استفاده:

$voiceID = "397FB7F7-38A5-4748-A72B-352FF36B0D71";
$numbers = ["09120000000", "02112345678"];
$result  = $api->Call($voiceID, $numbers);
print_r($result);


نمونه پاسخ:

{
  "data": true,
  "success": true,
  "code": 1,
  "error": null,
  "message": null
}

8️⃣ وب سرویس OTP تلفنی (CallOTP)

توضیح: ارسال کد یکبار مصرف از طریق تماس تلفنی.

پارامترها:

code (string) – کد OTP

number (string) – شماره موبایل یا ثابت

نمونه استفاده:

$code   = "12345";
$number = "09121112222";
$result = $api->CallOTP($code, $number);
print_r($result);


نمونه پاسخ:

{
  "data": true,
  "success": true,
  "code": 1,
  "error": null,
  "message": null
}

9️⃣ وب سرویس OTP تلفنی alt (CallOTPalt)

توضیح: نسخه پشتیبان CallOTP با شبکه جداگانه.

پارامترها:

code (string) – کد OTP

number (string) – شماره موبایل یا ثابت

نمونه استفاده:

$code   = "1234";
$number = "09121112222";
$result = $api->CallOTPalt($code, $number);
print_r($result);


نمونه پاسخ:

{
  "data": true,
  "success": true,
  "code": 1,
  "error": null,
  "message": null
}

🔟 وب سرویس OTP پیامکی (SmsOTP)

توضیح: ارسال کد یکبار مصرف یا رمز به شماره موبایل از طریق پیامک با خط ۸ رقمی.

پارامترها:

code (string) – کد OTP

mobile (string) – شماره موبایل

template (int, اختیاری) – نوع پیامک: 0=کد، 1=کد ورود، 2=کد تایید، 3=رمز، 4=رمز ورود (پیش‌فرض: 1)

نمونه استفاده:

$code   = "123456";
$mobile = "09120000000";
$template = 1;
$result = $api->SmsOTP($code, $mobile, $template);
print_r($result);


نمونه پاسخ:

{
  "data": true,
  "success": true,
  "code": 1,
  "error": null,
  "message": null
}


✅ این فایل آماده است و کافی است به صورت README.MD ذخیره شود.